jQuery Functions Used

This project uses jQuery to create interactivity:

click() → runs code when the button is clicked
hover() → detects mouse enter/leave
animate() → adds smooth animations
slideToggle() → shows/hides content with animation
css() → changes styles dynamically
text() → updates button text

▶️ How to Run

Open index.html in your browser.
Author: Hosni Ahmad